FROM public.ecr.aws/lambda/python:3.13

COPY src/ ${LAMBDA_TASK_ROOT}/src

COPY api_modules.txt ${LAMBDA_TASK_ROOT}

RUN pip install --no-cache-dir -r api_modules.txt

CMD [ "src.api.endpoint.handler" ]
